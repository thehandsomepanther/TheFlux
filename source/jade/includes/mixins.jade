mixin meta(title, description, image, twittersite, twitterauthor, url)
	// Meta
	meta(name="description", content="#{description}")
	meta(name="author", content="Northwestern University Medill School of Journalism")
	
	// Twitter Card https://dev.twitter.com/cards/types/summary
	meta(name="twitter:card", content="summary")
	meta(name="twitter:site", content="#{twittersite}")
	meta(name="twitter:creator", content="#{twitterauthor}")
	meta(name="twitter:title", content="#{title}")
	meta(name="twitter:description", content="#{description}")
	meta(name="twitter:image", content="#{image}")

	// Facebook Open Graph https://developers.facebook.com/docs/sharing/webmasters#markup
	meta(property="og:url", content="#{url}")
	meta(property="og:type", content="article")
	meta(property="og:title", content="#{title}")
	meta(property="og:description", content="#{description}")
	meta(property="og:image", content="#{image}")

mixin head(title)
	include ascii

	head
		meta(charset='utf-8')
		meta(http-equiv="X-UA-Compatible", content="IE=edge")
		meta(name='viewport', content='width=device-width, initial-scale=1.0')
		meta(name='description', content='Starter Kit for Bootstrap')
		meta(name='author', content='Zach Wise')
		meta(name='keywords', content='Template, Bootstrap')
		meta(name='apple-mobile-web-app-capable', content='yes')
		meta(name='apple-touch-fullscreen', content='yes')

		title #{title}

		// CSS
		link(rel='stylesheet', href='css/project.css?v10')

		// HTML5 shim, for IE6-8 support of HTML elements
		//if lt IE 9
			script(src='https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js')
			script(src='https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js')
		
		// Icon
		link(rel='shortcut icon', href='img/ico/favicon.png')
		link(rel='apple-touch-icon-precomposed', href='img/ico/favicon.png')
		link(rel='apple-touch-icon-precomposed', href='img/ico/apple-touch-icon-144.png', sizes="144x144")


mixin navbar(story)
	if story
		- var path = "../"
	else 
		- var path = ""
		
	// NAVBAR
	header.navigation.navbar-fixed-top#navbar
		div.navbar.navbar-default.storybar#navbar-story(role="navigation")

			div.row
		div#navbar-product.navbar.navbar-default(role="navigation")
			div.container-fluid
				div.navbar-header
					button.navbar-toggle(type="button", data-toggle="collapse", data-target="#product-navbar-collapse")
						span.sr-only Toggle navigation
						span.icon-bar
						span.icon-bar
						span.icon-bar
				div.collapse.navbar-collapse.navbar-left#product-navbar-collapse
					ul.nav.navbar-nav
						li.dropdown
							a.dropdown-toggle.navbar-brand(href="#", data-toggle="dropdown")
								| The Flux
								b.caret
							ul.dropdown-menu
								li
									div.container.about-knightlab
										div.row
											div.col-sm-12
												h3 Stories on Transition
												p TK What do our identities cost us? That's the question six teams of graduate journalism students asked as they tackled six different cultures and six different economies. The result: Six interactive articles that explore what we pay in money, time, energy, and missed opportunities for every choice we make.
						li
							a#stories-btn(href="#")
								span.icon-stack(style="margin-right:5px;")
								| More Stories
						li#navbar-title
							| The Flux
				div.collapse.navbar-collapse.navbar-right
					ul.nav.navbar-nav
						li
							a.navbar-brand(href="#{path}/index.html")
								img.img-responsive(src="#{path}img/logo_small.png")

	// END NAVBAR